
为什么要面向接口编程：
  > 降低耦合度，提高扩展力


  impl 表示接口的实现包
  AccountDaoImpl 实现的类后接上 Impl 表示实现类

目前项目仍然存在缺陷：
1. 在service 层控制了事务，service方法中事务控制代码看着有点变扭，以后能不能不写？
  > 可以使用动态代理机制解决这个问题
2.目前虽然面向接口编程了，但是并没有完全解决对象和对象之间的依赖关系，怎么办？
  > 可以使用Spring的IOC容器来解决这个问题。